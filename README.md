This is a simple (as simple as I can make it)
OS interface header file, used to interface
with the file system of an operating system.

Currently, only Windows 10 and Linux Debian
are being officially supported.
